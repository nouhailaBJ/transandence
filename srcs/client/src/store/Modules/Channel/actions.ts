import { Logger } from "@/common/helpers/Logger";
import { FriendsState } from "@/types/user";
import { ActionContext } from "vuex";

// actions
const actions = {
  // declineRequest(context: any, user: any) {
  //   context.commit("declineRequest", user);
  // },
};

export default actions;
